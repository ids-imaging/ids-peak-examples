repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        exclude: ^data/
      - id: detect-private-key
      - id: check-case-conflict

  # Python linting & formatting
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.1.6
    hooks:
      - id: ruff
        args: [--fix]  # optional: auto-fix style issues

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.19.1
    hooks:
      - id: mypy
        args: [--ignore-missing-imports, --explicit-package-bases, --config-file, python/mypy.ini]
        additional_dependencies:
          [
            ids-peak-common,
            ids-peak-ipl,
            ids-peak-icv>=1.3,
            ids-peak>=1.15,
            ids-peak-afl
          ]


  # C/C++ clang-format
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v19.1.7
    hooks:
      - id: clang-format
        files: \.(c|cpp|h|hpp)$

   # C# format
  - repo: local
    hooks:
    - id: dotnet-format
      name: dotnet format
      entry: dotnet format csharp/PeakExamples.sln
      language: system
      require_serial: true
      types: [c#]
      pass_filenames: false

  # check if readme lists all available examples
  - repo: local
    hooks:
      - id: check-readme-examples
        name: Check README example folders
        entry: python .pre-commit-hooks/check_readme_examples.py
        language: system
        always_run: true
        pass_filenames: false

  - repo: local
    hooks:
      - id: validate-sln
        name: Validate Visual Studio solution files
        entry: python .pre-commit-hooks/check_solution_files.py
        language: system
        files: \.sln$

  - repo: local
    hooks:
        - id: check-readme-links
          name: Check if README URLs are available
          entry: python .pre-commit-hooks/check_readme_links.py
          language: system
          files: (?i)readme.*\.(md|rst|txt)$

  - repo: local
    hooks:
        - id: forbidden-words
          name: Check forbidden words
          entry: python .pre-commit-hooks/check_forbidden_words.py
          language: system
